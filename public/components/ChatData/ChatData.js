'use strict';

export class ChatData {
    constructor() {
    
    }

    render() {
        const outer = document.createElement('div');
        outer.classList.add('chat-data');

        return outer;
    }
}
